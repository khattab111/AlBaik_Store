@extends('layouts.app')
@section('title', $order->order_number)
@section('content')
<section class="mx-auto max-w-5xl px-4 py-10"><h1 class="mb-6 text-3xl font-bold">{{ $order->order_number }}</h1><div class="mb-6 rounded-xl border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><p>{{ __('Status') }}: {{ __($order->status) }}</p><p>{{ __('Total') }}: {{ number_format((float)$order->total,2) }} USD</p><p>{{ __('Shipping Address') }}: {{ $order->shipping_country }} / {{ $order->shipping_city }} / {{ $order->shipping_town }} / {{ $order->shipping_street }}</p><p>{{ __('Phone') }}: {{ $order->customer_phone }} - {{ __('WhatsApp') }}: {{ $order->customer_whatsapp }}</p>@foreach($order->payments as $payment)<p>{{ __('Payment Status') }}: {{ __($payment->status) }} @if($payment->receipt_image)<a class="text-red-700" href="{{ asset('storage/'.$payment->receipt_image) }}" target="_blank">{{ __('Payment Receipt') }}</a>@endif</p>@endforeach</div><div class="grid gap-3">@foreach($order->items as $item)<div class="rounded-xl border bg-white p-4 dark:border-slate-800 dark:bg-slate-900">{{ $item->product->name }} × {{ $item->quantity }} = {{ number_format((float)$item->total_price,2) }} USD</div>@endforeach</div></section>
@endsection
